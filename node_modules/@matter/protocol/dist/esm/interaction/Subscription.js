/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { hex } from "#general";
var Subscription;
((Subscription2) => {
  function idStrOf(subscription) {
    if (typeof subscription === "object") {
      subscription = subscription.subscriptionId;
    }
    if (subscription === void 0) {
      return void 0;
    }
    return hex.fixed(subscription, 8);
  }
  Subscription2.idStrOf = idStrOf;
  function diagnosticOf(subscription) {
    return {
      "sub#": idStrOf(subscription)
    };
  }
  Subscription2.diagnosticOf = diagnosticOf;
})(Subscription || (Subscription = {}));
export {
  Subscription
};
//# sourceMappingURL=Subscription.js.map
